# Drone_Pro_Max

## Setup

Install uv:

    curl -Ls https://astral.sh/uv/install.sh | sh

Then run:

    uv sync

Run the project:

    uv run python file.py

In pyproject.toml there is a project version number, change it accordingly depending on the change you made